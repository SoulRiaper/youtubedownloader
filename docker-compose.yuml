version: "3"

services:

  app: # Имя сервиса
    build:
      dockerfile: Dockerfile
    ports: # Проброс портов
      - "8081:8081"
    depends_on:
      - redis

  redis:
    # Имя образа. Здесь мы используем базу данных Postgres
    image: redis:latest
    environment:

    volumes: